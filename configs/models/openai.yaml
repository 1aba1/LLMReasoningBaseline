model_type: openai                 # 模型类型
model_name: gpt-4o-mini            # 模型名称，对应 API 的 model 字段
base_url: https://api.openai.com/v1  # OpenAI 官方接口地址；为空则使用 SDK 默认
api_key_env: OPENAI_API_KEY        # 从哪个环境变量读取 API Key
max_tokens: 2048
temperature: 0.0
